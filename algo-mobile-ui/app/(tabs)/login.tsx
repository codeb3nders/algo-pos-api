import { View, Text } from '../../components/Themed';

const LoginScreen = () => {
  return (
    <View>
      <Text className="text-green-300">Login</Text>
    </View>
  );
};

export default LoginScreen;
